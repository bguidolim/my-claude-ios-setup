/// iOS tech pack-specific string constants.
/// Keeps the pack self-contained and independent from core.
enum IOSConstants {

    // MARK: - File Names

    enum FileNames {
        /// The XcodeBuildMCP configuration directory name.
        static let xcodeBuildMCPDirectory = ".xcodebuildmcp"
    }
}
